# 신규 가입 후 티저 진입 수정 체크리스트

- [x] 신규 가입 성공 분기에서 localStorage의 `beyondus_app_open_date`를 먼저 읽는 문제 확인.
- [x] GAS `registerUser()` 응답에 `parish`, `isStaff`, `isDev`, `appOpenDate` 추가.
- [x] 프론트 회원가입 성공 분기에서 서버 응답 기반으로 저장 및 앱 진입 판단.
- [x] `APP_VERSION`, `app.html`, `sw.js`, `version.txt` 버전 동기화.
- [x] 문법 검사와 diff 검사 실행.
- [x] 커밋 후 `main`에 푸시.
