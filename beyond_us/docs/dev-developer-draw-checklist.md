# DEV 개발자 무제한 뽑기 체크리스트

- [x] 현재 DEV 뽑기 UI와 GAS `drawCard` 우회 조건을 확인한다.
- [x] 작업 계획과 판단 근거를 문서로 남긴다.
- [x] GAS에 개발자 계정 판별 헬퍼를 추가한다.
- [x] GAS `drawCard`에서 개발자 테스트 뽑기 시 `ticket.granted`를 먼저 기록한다.
- [x] 프론트에서 현재 로그인 계정이 개발자일 때만 무제한 뽑기 UI를 표시한다.
- [x] 프론트 요청의 `testMode`를 현재 개발자 계정 여부로 좁힌다.
- [x] `APP_VERSION`, `version.txt`, `sw.js`, `app.html` 버전을 동기화한다.
- [x] 문법 검사와 diff 검사를 실행한다.
- [x] 변경 사항을 커밋하고 `dev`에 푸시한다.
