# 관리자 천로역정 QR 개별 인쇄 전환 계획

관리자 개발자 탭의 천로역정 QR 개별 버튼은 PNG 저장 대신 전체 인쇄와 같은 새 창 인쇄 흐름을 사용한다.

1. 기존 전체 인쇄 로직을 한 개 QR에도 재사용할 수 있게 분리한다.
2. QR 카드의 `PNG 저장` 버튼을 `인쇄` 버튼으로 바꾼다.
3. 개별 `인쇄` 버튼은 해당 QR 한 장만 새 창에 띄우고 인쇄 창을 연다.
4. 더 이상 쓰지 않는 PNG 저장 전용 함수는 제거한다.
5. 문법 검증 후 main에 반영한다.
