# 관리자 천로역정 QR PNG 열기 수정 계획

관리자 개발자 탭의 천로역정 QR `PNG 열기` 버튼이 실제 운영 출력물로 쓰일 수 있게 수정한다.

1. 기존 `PNG 열기`가 외부 QR 이미지 URL만 새 창으로 여는 구조인지 확인한다.
2. QR 이미지를 canvas에 그려서 PNG data URL을 생성한다.
3. PNG 상단에 `천로역정 스팟 n / 이름 / 인증 QR` 제목과 간단한 안내를 넣는다.
4. 버튼 호출부에서 스팟 번호와 이름을 전달한다.
5. 문법 검증 후 main에 반영한다.
