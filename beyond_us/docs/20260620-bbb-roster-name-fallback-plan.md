# BBB roster name fallback plan

Goal.

- Make BBB care buddy and secret buddy names always prefer the roster participant name.
- Treat literal `null` and `undefined` strings as empty values on the server.
- Keep profile data as fallback only when roster names are missing.
- Bump app cache version so users do not keep stale BBB rendering code.
