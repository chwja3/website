# B.B.B. 이름 표시 정리 컨텍스트

## 결정

- 사용자가 보는 관리 화면에서는 닉네임보다 이름을 우선한다.
- 문자열 `"null"`과 `"undefined"`는 실제 값이 없는 것으로 취급한다.
- 케어버디 저장과 검색 안정성을 위해 닉네임과 ID는 검색 후보에는 유지하되, 기본 화면 표시에서는 이름과 조/교구/참석 구분을 중심으로 보여준다.
- `displayName`은 닉네임처럼 쓰이는 경우가 있어 이름 표시 fallback에서 제외했다. 이름이 없으면 닉네임으로 대체하지 않고 `이름 없음`으로 표시한다.
