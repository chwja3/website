# 새 버전 배너 반복 수정 계획

앱이 새로고침 후에도 계속 새 버전 배너를 띄우는 문제를 해결한다.

1. 앱 로컬 버전, `version.txt`, service worker 캐시 버전을 비교한다.
2. 서로 다른 버전 값을 현재 앱 버전으로 동기화한다.
3. 원격 버전이 로컬보다 최신일 때만 배너가 뜨도록 비교 조건을 보강한다.
4. 문법 검증 후 DEV에 반영한다.
