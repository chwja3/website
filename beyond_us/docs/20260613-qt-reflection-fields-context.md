# 2026-06-13 QT 답변 입력칸 정책 컨텍스트

## 배경

- 2026년 6월 20일 오후와 2026년 6월 21일 오전 QT에는 질문이 3개 있다.
- 사용자는 각 질문 답변 3개와 기도제목을 입력할 수 있어야 한다.
- 2026년 6월 19일까지는 입력칸 없이 기존처럼 날짜별 QT 이미지만 보여야 한다.

## 결정

- 입력 가능 날짜는 `2026-06-20`, `2026-06-21` 두 날짜로 제한한다.
- 답변칸은 세 개를 별도 textarea로 둔다.
- 기존 `answer_text`는 답변 1 저장 위치로 유지하고, `answer2_text`, `answer3_text`를 추가한다.
- 앱은 새 `submit_qt_reflection_v2` RPC를 호출한다.

## 주의점

- 기존 제출 데이터가 있다면 `answer_text`가 답변 1로 보인다.
- 날짜별 QT 이미지 로딩과 fallback 경로는 건드리지 않는다.

## 검증 기록

- `node --check beyond_us\app.js` 통과.
- 충돌 표식 검색 결과 없음.
- `git diff --check` 통과. 줄바꿈 경고만 확인됨.
