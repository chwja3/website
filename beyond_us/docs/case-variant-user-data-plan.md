# Case Variant User Data Plan

This plan was superseded by `case-sensitive-user-id-plan.md`.

The earlier direction allowed limited fallback from a case-variant user id to an active canonical user. The current decision is stricter. User ids are case-sensitive, so `Oh! New` and `oh! New` are different users.
