# 2026-05-31 H&P/Q.T. 긴급 보정 계획

## 목표
- H&P 정답 입력에서 `김해솔` 같은 전체 이름뿐 아니라 `해솔`처럼 성을 뺀 이름도 정답으로 인정한다.
- 기존에 오답으로 저장된 H&P 응답을 새 규칙으로 재계산하고, w3/w6 정답자는 카드 뽑기권을 중복 없이 지급한다.
- Q.T. 이미지 로딩은 Supabase Storage, 현재 상대 경로, 앱 기준 절대 경로를 순서대로 시도해서 일부 사용자에게 "준비중"이 뜨는 상황을 줄인다.

## 검증
- SQL 마이그레이션에 정답 비교 함수, submit/get/recalculate 반영 여부를 정적 확인한다.
- 앱 버전, app.html 쿼리, sw cache, version.txt를 같은 값으로 맞춘다.
- `git diff --check`로 공백 오류를 확인한다.
