# BBB manual profile reassign plan

Goal.

- Let admins resolve duplicate roster matches even when the selected profile is already attached to another roster row.
- Move the profile to the newly confirmed row and mark the old row as manually unmatched.
- Keep the existing name-match guard so admins cannot attach a profile with a different real name.
