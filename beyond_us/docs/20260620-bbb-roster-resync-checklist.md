# B.B.B. 신규 가입자 조 매칭 재동기화 체크리스트

- [x] 기존 B.B.B. 조 매칭 함수와 trigger 구조를 확인한다.
- [x] PROD SQL Editor에서 실행할 재동기화 SQL을 만든다.
- [x] SQL에 실행 결과와 남은 미매칭 진단 쿼리를 포함한다.
- [ ] PROD SQL Editor에서 실행한다.
- [ ] admin B.B.B. 매칭 화면에서 `닉네임 없음` 숫자를 확인한다.
